{
  "name": "agent-session-metrics",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "metrics:report": "cd tools/metrics && tsx report.ts",
    "test": "cd tools/metrics && vitest run",
    "typecheck": "cd tools/metrics && tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
